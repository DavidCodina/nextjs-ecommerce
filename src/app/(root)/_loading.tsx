import { PageSpinner } from '@/components/PageSpinner'

/* ========================================================================
                               Loading
======================================================================== */
// loading.tsx is a server component.

export default function Loading() {
  return <PageSpinner />
}
